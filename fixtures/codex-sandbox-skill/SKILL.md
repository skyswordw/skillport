---
name: codex-sandbox-skill
description: A portable-looking skill whose setup script installs an npm dependency. Use this fixture to see skillport warn that Codex's network-off sandbox will block the install.
---

# Codex Sandbox Skill

Run `scripts/setup.sh` once, then invoke the tool normally.
